<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Search Results for <%= query %></title>
  <link rel="stylesheet" href="/style.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
    integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA=="
    crossorigin="anonymous" referrerpolicy="no-referrer" />
</head>

<body>
  <%- include("navbar") %>

  <div class="container mt-4">
    <h1 class="display-5">Search Results for "<%= query %>"</h1>
    <div class="mt-5 row">
      <% if (products.length > 0) { %>
        <% products.forEach(product => { %>
          <div class="col-xl-3 col-lg-3 col-md-6 col-sm-12">
            <a id="uniqueid" class="text-decoration-none " href="/Face/<%= product.id %>">
              <div class="card mb-4">
                <img src="<%= product.imageUrl %>" class="card-img-top" alt="<%= product.title %>">
                <div class="card-body">
                  <h5 class="card-title">
                    <%= product.title %>
                  </h5>
                  <p class="card-text fs-3">$<%= product.price %>
                  </p>
                </div>
              </div>
            </a>
          </div>
        <% }); %>
      <% } else { %>
        <p>No products found matching your search criteria.</p>
      <% } %>
    </div>
  </div>
    
  <% if (totalPages > 1) { %>
    <div class="d-flex justify-content-center mt-4">
      <% for(let i = 1; i <= totalPages; i++) { %>
        <a class="text-decoration-none" href="/search/<%= i %>?query=<%= query %>">
          <button class="btn <% if(i === page) { %>btn-danger<% } else { %>btn-outline-danger<% } %> mx-1">
            <%= i %>
          </button>
        </a>
      <% } %>
    </div>
  <% } %>

  <!-- <footer>
    <div class="foot d-flex justify-content-center align-item-center ">
      <p>&copy;2024 Miss Rose-Official Store.com</p>
    </div>
  </footer> -->

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
    crossorigin="anonymous"></script>
  <script src="/JS/script.js"></script>
</body>

</html>
